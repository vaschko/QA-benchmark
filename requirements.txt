# Bereits in der Umgebung vorhanden: requests, httpx, pydantic, pyyaml, typer, rich
# Hier nur die fehlenden Abhängigkeiten:
PyMuPDF>=1.24
python-docx>=1.1
anthropic>=0.40
openai>=1.0
langdetect>=1.0.9
